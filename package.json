{
  "name": "dynamic-map-card",
  "version": "0.1.0",
  "description": "A Home Assistant Lovelace map card wrapper that dynamically resolves theme_mode from an entity state.",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.11"
  }
}

